package org.dave.example.server;

public class Test_TodoistAPITaskSource {

 /*   @Test
    public void test_getInputStream() throws Exception {
        AuthenticationModule authenticationModule = new StaticAuthenticationModule();
        TodoistAPITaskSource source = new TodoistAPITaskSource(authenticationModule);
        source.authenticate();
        String output = IOUtils.toString(source.getInputStream());
        assertTrue(output.contains("items")&&output.contains("content")&&output.contains("project_id")); //random selection
    }*/
}